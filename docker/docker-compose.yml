version: '3.7'

services:
  app:
    tty: true
    restart: always
    env_file: ./.env
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
      target: dev
    volumes:
      - ../app:/app:delegated
    ports:
      - ${APP_PORT}:3000
